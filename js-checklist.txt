1. Function is a set of instruction. Declaring function below:
    function myFunc(param){
        // code
        return value;
    }

2. A parameter is a special kind of variable used is a function to refer to one of the pieces of data provided input to the function.

3. A return statement is used when we need to export raw data from a function.

4. Empty return give undefined value.

5. We can return primitive values and object types by using the return statement. We can also return multiple values using the return statement.

6. Function Declaration: A function declaration also known as a function statement declares function a function keyword. The function declaration must have a
   function  name.

   Function Expression: A function expression is similar to a function declaration without the function name.

